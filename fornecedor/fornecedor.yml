spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: admin
    username: postgres
    url: jdbc:postgresql://localhost:5432/db_fornecedor
  jpa:
    hibernate:
      temp.use_jdbc_metadata_defaults: false
      database-platform: org.hibernate.dialect.PostgreSQL9Dialect
      ddl-auto: update
      show-sql: true
